% UML class diagram of HydroBayesCal, drawn with pgf-umlcd.sty
% UML started on: 2023-04-05, rewritten for v1.0.0 on 2026-07-02,
% Delft3D-FLOW binding added for v1.1.0 on 2026-07-03
% written by: Sebastian Schwindt
%
% The diagram shows the three cooperating layers:
%   1. full-complexity model bindings (HydroSimulations + solver subclasses)
%   2. surrogate model and Bayesian Active Learning (surrogate package)
%   3. post-processing (visualize package and the standalone extract module)
% The templates/ driver scripts wire layers 1 and 2 together.
%
% Compile with: pdflatex BayesCal-UML.tex

\documentclass{article}
\usepackage[margin=5mm,landscape,a1paper]{geometry}
\usepackage{hyperref}

\usepackage[
% school,
% simplified
]{pgf-umlcd}

\usepackage{listings}

\definecolor{listinggray}{gray}{0.92}
\lstset{ %
language=[LaTeX]TeX,
breaklines=true,
frame=single,
basicstyle=\footnotesize\ttfamily,
backgroundcolor=\color{listinggray},
keywordstyle=\color{blue}
}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  anchorcolor=black,
  citecolor=olive,
  filecolor=magenta,
  menucolor=red,
  urlcolor=blue
}

% redefine UML package colors so that it hurts less in the eyes
\renewcommand{\umlfillcolor}{gray_light}


\begin{document}
\section*{\texttt{\detokenize{HydroBayesCal v1.1}}}
\thispagestyle{empty}
\begin{tikzpicture}

	% ================================================================
	% LAYER 1: FULL-COMPLEXITY MODEL BINDINGS
	% ================================================================
	\begin{package}{hysim}
		\begin{abstractclass}[text width=11cm]{HydroSimulations}{0,0}
			\attribute{control_file: str}
			\attribute{model_dir, res_dir: str}
			\attribute{calibration_pts_file_path: str}
			\attribute{calibration_parameters: list, param_values: list}
			\attribute{calibration_quantities, extraction_quantities: list}
			\attribute{init_runs, max_runs, n_cpus: int}
			\attribute{observations, measurement_errors: np.array}
			\attribute{asr_dir, calibration_folder, restart_data_folder: str}
			\operation[0]{run\_multiple\_simulations(collocation\_points, ...): np.array}
			\operation[0]{run\_single\_simulation(control\_file, ...): None}
			\operation[0]{update\_model\_controls(new\_parameter\_values, ...): None}
			\operation[0]{output\_processing(output\_data\_path, ...): np.array}
			\operation[0]{extract\_data\_point(input\_file, calibration\_pts\_df, ...): None}
			\operation{set_observations_and_variances(...): None}
			\operation{set_calibration_parameters(params, values): None}
			\operation{read_data(results_folder, file_name): object}
		\end{abstractclass}
	\end{package}

	\begin{package}{telemac}
		\begin{class}[text width=11cm]{TelemacModel}{14,8}
			\inherit{HydroSimulations}
			\attribute{tm_xd: str (Telemac2d / Telemac3d)}
			\attribute{friction_file: str (.tbl)}
			\attribute{gaia_steering_file: str (.cas)}
			\attribute{results_filename_base: str}
			\operation{run_single_simulation(...): None}
			\operation{run_multiple_simulations(...): np.array}
			\operation{update_model_controls(...): None}
			\operation{rewrite_steering_file(...): int}
			\operation{output_processing(...): np.array}
			\operation{extract_data_point(...): None (depth-explicit 2D/3D)}
			\operation{tbl_creator(zone_identifier, val, ...): None}
		\end{class}
		\begin{object}[text width=11cm]{\detokenize{config_telemac}}{14,-6}
			\attribute{classification_tm_gaia_dict: dict}
			\attribute{slf_2d_variables_from_3d: list}
			\attribute{von_Karman_constant, kinematic_viscosity_water: float}
		\end{object}
	\end{package}

	\begin{package}{openfoam}
		\begin{class}[text width=11cm]{OpenFOAMModel}{14,-11}
			\inherit{HydroSimulations}
			\attribute{solver_name: str (interFoam)}
			\attribute{control_points: np.array}
			\operation{run_multiple_simulations(...): np.array}
			\operation{output_processing(...): np.array}
			\operation{save_calibration_data(it, ...): None}
		\end{class}
		\begin{class}[text width=11cm]{OpenFOAMController}{14,-19}
			\attribute{case_dir: str}
			\operation{update_model_controls(params): None}
			\operation{update_boundary_condition(...): None}
			\operation{decompose_parallel_case(nprocs) / reconstruct_parallel_case()}
			\operation{run_simulation(nprocs, solver): None}
			\operation{convert_to_vtk(): None}
			\operation{extract_fields_from_vtk(...): (coords, U, k)}
		\end{class}
	\end{package}

	\begin{package}{delft3d}
		\begin{class}[text width=11cm]{Delft3DModel}{14,-26.5}
			\inherit{HydroSimulations}
			\attribute{env_script: str (env.sh)}
			\attribute{d_hydro_config: str (config_d_hydro.xml)}
			\attribute{roughness_formulation: str (Chezy / Manning / WhiteColebrook)}
			\operation{run_multiple_simulations(...): np.array}
			\operation{update_model_controls(controller, params): None}
			\operation{output_processing(...): np.array}
			\operation{save_calibration_data(it, ...): None}
		\end{class}
		\begin{class}[text width=11cm]{Delft3DController}{14,-33.5}
			\attribute{case_dir, env_script: str}
			\attribute{mdf_file, case_name: str (properties)}
			\operation{update_mdf_parameter(keyword, value): None}
			\operation{set_roughness(value, formulation): None}
			\operation{ensure_netcdf_output(): None (NetCDF map output)}
			\operation{run_simulation(nprocs): None}
			\operation{check_run(): None}
			\operation{extract_map_results(n_avg_timesteps): (coords, fields)}
		\end{class}
	\end{package}

	\begin{package}{ppmodules}
		\begin{class}[text width=10cm]{ppSELAFIN}{28,8}
			\attribute{NBV1, NELEM, NPOIN, NDP, NPLAN: int}
			\attribute{vnames, vunits, time: list}
			\attribute{IKLE, IPOBO, x, y: np.array}
			\operation{readHeader() / writeHeader(): None}
			\operation{readTimes() / readVariables(t_des): None}
			\operation{readVariablesAtNode(node): None}
			\operation{writeVariables(time, temp): None}
			\operation{getMesh(): (NELEM, NPOIN, NDP, IKLE, IPOBO, x, y)}
			\operation{getNPLAN() / getVarNames() / getVarValuesAtNode(): ...}
		\end{class}
	\end{package}

	% ================================================================
	% LAYER 2: SURROGATE MODEL + BAYESIAN ACTIVE LEARNING
	% ================================================================
	\begin{package}{surrogate}
		\begin{class}[text width=10cm]{BayesianInference}{42,8}
			\attribute{model_predictions, observations, error: np.array}
			\attribute{prior, prior_log_pdf: np.array}
			\operation{calculate_likelihood(): None}
			\operation{calculate_likelihood_with_error(): None}
			\operation{rejection_sampling(): None}
			\operation{estimate_bme(): (BME, RE, ELPD, IE, post.)}
		\end{class}
		\begin{class}[text width=10cm]{SequentialDesign}{42,0}
			\attribute{exp_design, gpr: object}
			\attribute{al_strategy: str (dkl / bme / ie / global_mc)}
			\operation{run_sequential_design(prior_samples): np.array}
			\operation{bayesian_active_learning(y_mean, y_std, ...): dict}
			\operation{select_indexes(prior_samples, collocation_points): np.array}
		\end{class}
		\begin{class}[text width=10cm]{GPyTraining}{42,-8}
			\attribute{collocation_points, model_evaluations: np.array}
			\attribute{likelihood, kernel: gpytorch objects}
			\operation{train_(): None}
			\operation{predict_(input_sets, get_conf_int): dict}
		\end{class}
		\begin{class}[text width=10cm]{MultiGPyTraining}{42,-14.5}
			\attribute{multitask_selection: str (variables / locations / all)}
			\operation{train_tasks_variables() / _locations() / _all(): None}
			\operation{predict_(input_sets, get_conf_int, ...): dict}
		\end{class}
		\begin{class}[text width=10cm]{SklTraining}{42,-20.5}
			\operation{train_(): None}
			\operation{predict_(input_sets, get_conf_int): dict}
		\end{class}
		\begin{class}[text width=10cm]{Exploration}{42,-25}
			\operation{get_exploration_samples(): np.array}
		\end{class}
	\end{package}

	% ================================================================
	% LAYER 3: POST-PROCESSING
	% ================================================================
	\begin{package}{visualize}
		\begin{class}[text width=10.5cm]{BayesianPlotter}{56,8}
			\attribute{results_folder_path, save_folder: Path}
			\operation{plot_posterior_updates(...) / plot_prior_posterior_kde(...)}
			\operation{plot_bme_re(...) / plot_bme_3d(...) / plot_bme_comparison(...)}
			\operation{plot_model_comparisons(...) / plot_validation_results(...)}
			\operation{plot_metric_comparison(...) / location_metric_heatmap(...)}
			\operation{evaluate_calibration(...): (df_spatial, df_summary)}
			\operation{observed_vs_modeled_compare(...) / plot_residuals(...)}
		\end{class}
	\end{package}

	\begin{object}[text width=10.5cm]{extract}{56,-8}
		\operation{extract_results(result_file, variable, x, y, z, ...): pd.DataFrame}
	\end{object}

	\begin{object}[text width=10.5cm]{\detokenize{function_pool}}{56,-12}
		\operation{call_process(bash_command, environment): int}
		\operation{update_json_file(json_path, modeled_values_dict, ...): None}
		\operation{interpolate_values(coords, values, point): np.array}
		\operation{append_new_line(file_name, text_to_append): None}
	\end{object}

	% drivers note
	\umlnote[text width=10.5cm] (drivers) at (56,-18) {\textbf{templates/} driver scripts
		(\texttt{bal\_telemac.py}, \texttt{bal\_openfoam.py},
		\texttt{bal\_delft3d.py}) read a config
		file, instantiate a binding (layer 1), delegate the experimental design
		to BayesValidRox, and run the GPE training + BAL loop (layer 2).
		Post-processing (layer 3) works inside and outside this workflow.};

	% ================================================================
	% IMPORT ASSOCIATIONS
	% ================================================================
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(ppSELAFIN) --node[above, sloped, black]{import} (TelemacModel);
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(config_telemac) --node[above, sloped, black]{import} (TelemacModel);
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(OpenFOAMController) --node[above, sloped, black]{uses} (OpenFOAMModel);
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(Delft3DController) --node[above, sloped, black]{uses} (Delft3DModel);
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(Exploration) --node[above, sloped, black]{import} (SequentialDesign);
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(ppSELAFIN) --node[above, sloped, black]{import} (extract);
	\draw[umlcd style dashed line, opacity=0.58, line width=0.5mm, -stealth]
		(function_pool) --node[above, sloped, black]{import} (HydroSimulations);

\end{tikzpicture}

\end{document}
